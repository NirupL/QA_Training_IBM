services:
  redis-server:
    image: docker.io/redis
    volumes:
      - ./redis:/data
    command: [ "redis-server", "--save", "1 1", "--protected-mode", "no" ]
  node-app:
    restart: always
    build:
      context: .
      dockerfile: dockerfile
    image: docker.io/niruplokesh/visits
    ports:
      - "8081:8081"
